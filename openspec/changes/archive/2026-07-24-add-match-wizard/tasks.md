# Tasks: add-match-wizard

## 1. Backend — Plan State & DTOs

- [x] 1.0 Bump the `subx-cli` dependency in `src-tauri/Cargo.toml` from `1.7` to `1.8`: `apply_unique_target_paths()` (task 2.3, design D5) is public only from 1.8.0 — the `1.7` caret range cannot resolve it — and the 1.8 line also brings AI-tagged language suffixes and batch rename dedup. Refresh `Cargo.lock` with `cargo update -p subx-cli --precise 1.8.0`
- [x] 1.1 Declare the `sync` (and `rt`) `tokio` feature as a direct `[dependencies]` entry in `src-tauri/Cargo.toml` — `tokio` is currently only a transitive dependency of `tauri`, so `use tokio::sync::Mutex` and `tokio::spawn` will not resolve — then add `PlanState` storage to `state.rs` as `tokio::sync::Mutex<HashMap<PlanId, PlanState>>` holding `Vec<MatchOperation>`, audit data, and the step-1 `CollectedFiles` (for `archive_origin()` lookups), plus the running-analysis `JoinHandle`; new analysis invalidates prior plans
- [x] 1.2 Define DTOs in `dto.rs`: `SourceScanResult`, `MatchPlanDto` (video-grouped operations with IDs, target paths, confidence, reasoning; unmatched videos/subtitles lists), `ExecutionReportDto` (from `Vec<OperationOutcome>`). **No TypeScript mirrors** — `add-typed-ipc-bindings` has landed, so each type derives `specta::Type` and its declaration is generated into `src/types/bindings.ts`; hand-writing one would be caught by that change's drift gate and the raw-`invoke` lint. Two constraints that change apply to every new DTO: no 64-bit integer may cross the boundary (specta rejects `u64`/`i64` as lossy in JavaScript — use `u32`, or a string for anything genuinely large), and `#[serde(skip_serializing_if)]` is unavailable (it is incompatible with the unified serde mode the bindings use), so an absent value travels as an explicit `null`
- [x] 1.2a Type the analysis progress as a `MatchProgress` DTO (with a `MatchStage` enum) carried over a `tauri::ipc::Channel<MatchProgress>` passed into `analyze_sources`, **not** a `tauri_specta::Event`. Emitting an event needs a runtime-generic `AppHandle<R>` in the command, and a generic command cannot be registered in the single generic `specta_builder` — the macro cannot infer `R` (design D2). `Channel` is runtime-agnostic and keeps the command non-generic; specta generates `Channel<MatchProgress>` in the bindings. Leave `collect_events![]` empty
- [x] 1.3 Extend `error.rs` with match error codes (`match.ai_not_configured`, `match.ai_unavailable`, `match.sources_insufficient`, `match.analysis_in_progress`, `match.stale_plan`, per-item execution failure codes mapped from `OperationError`) and their `en`/`zh-TW` strings

## 2. Backend — Commands

- [x] 2.1 Implement `list_source_files(paths)` using `InputPathHandler`/`CollectedFiles` (folders, files, archives — NOT bare `FileDiscovery`, which has no archive support) returning `SourceScanResult`
- [x] 2.2 Implement `analyze_sources(paths, relocation_mode)` as a spawned tokio task: build `MatchConfig` manually with the chosen `FileRelocationMode` (mirroring the CLI's `execute_with_client`; `ComponentFactory::create_match_engine()` hardcodes `None` and cannot be used), create the engine via `MatchEngine::new(create_ai_provider()?, match_config)`, call `match_file_list_with_audit()`, emit `match-progress` events (`scanning`/`analyzing`/`finalizing`), store the plan, return `MatchPlanDto`; reject with `match.analysis_in_progress` if a task is already running
- [x] 2.3 After analysis, apply the archive-origin rewrite (force copy to the video's directory for operations whose subtitle has an `archive_origin()` and no relocation yet, mirroring `match_command.rs`) and then run `apply_unique_target_paths()` before storing the plan
- [x] 2.4 Compute `unmatched_videos`/`unmatched_subtitles` by set difference against the scan result (operations ∪ rejected do not cover files the AI never mentioned; `rejected` is empty on cache hits)
- [x] 2.5 Implement `cancel_analysis()` aborting the stored task and clearing the pending plan
- [x] 2.6 Implement `execute_selected(plan_id, operation_ids)`: reject stale/unknown plan IDs with `match.stale_plan`, run `execute_operations_audit()` (NOT `execute_operations()`, which aborts on first failure) on the selected subset, build `ExecutionReportDto` from the outcomes
- [x] 2.7a Register every new command in `bindings.rs`'s single `collect_commands![…]` — not in a `tauri::generate_handler!`, which a guard test now rejects outright — and add each one to `ipc_tests.rs`'s `COMMANDS` allowlist, since the mock runtime starts with an empty ACL and denies anything unlisted
- [x] 2.7 Backend tests with `TestConfigService` + `wiremock` AI mock: full analyze→select→execute flow, relocation modes (incl. archive-origin forced copy), cancellation, concurrent-analysis rejection, stale plan, unconfigured provider, per-item failure collection

## 3. Frontend — Wizard

- [x] 3.1 Implement the wizard state machine (step transitions, plan invalidation with confirm when navigating back past Analysis) inside `WizardShell`; wire the home hub's Match card to it
- [x] 3.2 Step 1 Sources: multi-source drag-and-drop + browse, source list management, relocation mode selector (default rename in place), scan preview via `list_source_files`, insufficient-sources blocking state
- [x] 3.3 Step 2 Analysis: staged progress indicator driven by `match-progress` events, cancel button, error states (not configured → link to Settings; unavailable → retry)
- [x] 3.4 Step 3 Review: video-grouped operation list (videos with ≥1 match only) with target paths, confidence, and expandable reasoning; unmatched videos/subtitles sections; select-all default with per-operation checkboxes
- [x] 3.5 Step 4 Execute: plan summary (selected count + mode), execute action disabled on empty selection, per-item outcome report with success/failure styling
- [x] 3.6 Add `match` locale namespace with complete `en` and `zh-TW` strings

## 4. Tests & Verification

- [x] 4.1 Component tests (IPC mocked): state machine transitions, selection logic (incl. empty-selection disable), unmatched rendering, progress event handling, stale-plan recovery
- [x] 4.1a Register every new match-wizard screen component in the `SCREENS` list of `src/i18n/hardCodedStrings.test.tsx` — the test's own comment flags this as `add-match-wizard`'s responsibility, and an unregistered screen escapes the "no hard-coded string" gate entirely
- [ ] 4.2 Manual end-to-end verification with a real media folder and a local LLM (Ollama): mixed sources incl. an archive, multi-language matches, deselection, copy mode, mode change via back-navigation (should hit the crate cache), induced per-item failure. This is the one scenario automation cannot reach; if any scenario ends up here, waive it in `scripts/spec-coverage.config.json` with a `reason` and a `manualVerification` pointer (see the existing NVIDIA/Wayland waiver)
- [x] 4.3 Annotate each new `#### Scenario:` with a `// @covers <capability>/<requirement-slug>#<scenario-slug>` line comment directly above its verifying test — copy the IDs verbatim from `npm run spec:trace -- --report`, never hand-build them (a slug typo is a dangling annotation that fails the gate)
- [x] 4.4 Sync the `match-workflow` delta from `openspec/changes/add-match-wizard/specs/` into `openspec/specs/match-workflow/spec.md` so the traceability checker (which reads `openspec/specs` only) sees the new scenarios — without this every `@covers` above is dangling and `npm run verify` cannot pass. This is why the change is archived later with `openspec archive --skip-specs` (see AGENTS.md)
- [x] 4.5 `npm run verify` clean end to end: `tsc --noEmit`, both coverage gates at the 85 % floor, `spec:trace`, and `bindings:check` (run `npm run bindings:generate` and commit `src/types/bindings.ts` first). Also `cargo clippy` clean
