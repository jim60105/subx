# Tasks: add-translate-wizard

## 1. Backend — State & DTOs

- [x] 1.1 Add `TranslateState` to `state.rs`: single active plan (items + the scan's `CollectedFiles`, whose temp dirs keep archive-extracted inputs readable at execution time — design D1), executor slot reserved before spawn, epoch bumped by cancel. If `add-convert-wizard` has landed first, extracting a shared single-slot helper with `MatchState`/`ConvertState` is allowed but not required. Wire `AppState::translate_state()`
- [x] 1.2 Define DTOs in `dto.rs`: `TranslateOptionsDto` (target/source language, context, glossary text, output mode, overwrite), `TranslatePlanDto` (plan ID + items: id, input path, resolved output path, cue count, flags: `outputExists`, `outputCollision`, `replaceArchiveForbidden`, `unparsable`), `TranslateProgress` (index, total, file name, terminal stage), `TranslationReportDto` (per-item outcomes incl. empty-cue-fallback warnings). All `specta::Type`; no `u64`/`i64` crosses IPC (cue counts as `u32`); absent optionals travel as explicit `null`
- [x] 1.3 Extend error mapping with `translate.no_subtitles`, `translate.no_target_language`, `translate.stale_plan`, `translate.translation_in_progress`, `translate.ai_not_configured` (hint code → Settings), and item reason codes `translate.output_exists`, `translate.output_collision`, `translate.replace_archive_forbidden`, `translate.unparsable`; add `codes.`/`hints.` locale entries in **both** `en` and `zh-TW` `errors` namespaces

## 2. Backend — Commands

- [x] 2.1a Implement `list_translate_inputs(paths)`: the Step 1 scan — collect via `InputPathHandler`/`CollectedFiles`, `translate.no_subtitles` on an empty scan, per-file cue count with unparsable files flagged rather than failing, plus the configured `translation.default_target_language` for Step 2 to prefill. Takes **no options**: a plan cannot be resolved without a target language and Step 1 is before the field that supplies one, so fusing this into `preview_translation` dead-ends the wizard on any install without a configured default (design D1)
- [x] 2.1 Implement `preview_translation(paths, options)`: collect via `InputPathHandler`/`CollectedFiles`; `translate.no_subtitles` on an empty scan; parse each file for its cue count, flagging unparsable files instead of failing (design D1); resolve every output mirroring the CLI's private logic — suffix `<stem>.<lang>.<ext>` beside the input, beside the archive for extracted files, replace mode returning the input path, `translate.replace_archive_forbidden` for archive-extracted items in replace mode — with comments citing `translate_command.rs` (design Context); flag `outputExists` in suffix mode; detect intra-plan output collisions (archive entries sharing a basename resolve beside the archive to one path) and flag all but the first per output path with `translate.output_collision`, never re-included by the overwrite toggle (design D4); resolve the target language with CLI precedence (per-run value, else `translation.default_target_language`, else `translate.no_target_language` — design D5); store the plan, return `TranslatePlanDto`
- [x] 2.2 Implement `execute_translation(plan_id, item_ids, channel)`: reject stale IDs (`translate.stale_plan`); reserve the slot first (`translate.translation_in_progress`); `service.reload()`, build the provider once through the `AiProviderFactory` seam mapping failure to `translate.ai_not_configured`, then `TranslationEngine::new(provider, translation.batch_size)` (design D5); parse the glossary with the crate's public `parse_glossary_text()` and build one `TranslationRequest` per run; per item: `load_subtitle` → `translate_subtitle` → replace-mode backup (`<name>.<ext>.backup`, only when `general.backup_enabled`) → `save_subtitle`, writing only after full success (design D2); catch per-item failures into the report and continue; emit `TranslateProgress` per file behind the `ProgressReporter` seam; surface the engine's empty-cue fallback as a per-item warning (design D6); defensively refuse flagged-excluded items
- [x] 2.3 Implement `cancel_translation()` aborting the task and bumping the epoch: the in-flight file's pending HTTP request is dropped and that file is left unwritten; completed outputs remain (design D3)
- [x] 2.4 Register all four commands in `bindings.rs`'s single `collect_commands![…]` and add each to `ipc_tests.rs`'s `COMMANDS` allowlist
- [x] 2.5 Backend tests with `TestConfigService`, a scripted `AIProvider` (the match tests' pattern — the engine expects terminology + translation responses echoing cue IDs), `tempfile`, and `zip`: preview naming (suffix, archive-beside-archive, replace, forbidden-replace, `outputExists`, unparsable flag, output collision from an archive with duplicate basenames), target-language precedence incl. `translate.no_target_language`, glossary entries reaching the request, full execute flow preserving timing and cue count, per-item failure isolation, replace-with-backup on/off, cancellation abandoning the in-flight file, unconfigured provider, stale plan, concurrent-execution rejection

## 3. Frontend — Wizard

- [x] 3.1 Add `"translate"` to `ScreenId`, wire the home hub's Translate card (enabled — every hub card is now live; update `HomeScreen.test.tsx`, removing the coming-soon expectations), and implement `useTranslateWizard` (step machine; back past Translate cancels and invalidates via the generation ref)
- [x] 3.2 Step 1 Sources: drag-and-drop + browse, scan preview with per-file cue counts and a total, unparsable files visibly flagged, `translate.no_subtitles` blocking state
- [x] 3.3 Step 2 Options: target language prefilled from config (required — inline `translate.no_target_language` blocking), source language, context textarea, glossary textarea with format hint (`source = target` per line), output mode radio (suffix default / replace with backup note reflecting `general.backup_enabled`), overwrite toggle re-including `outputExists` items, per-item selection list showing resolved outputs and flags; the one-extra-request terminology note is stated copy
- [x] 3.4 Step 3 Translate: progress driven by the `TranslateProgress` channel (per-file only — no invented percentages), cancel button, elapsed time
- [x] 3.5 Step 4 Report: per-item outcomes with output paths, backup paths in replace mode, empty-cue warnings, and failure codes with hints (not-configured → Settings link)
- [x] 3.6 Add the `translate` locale namespace with complete `en` and `zh-TW` strings; register every new screen component in `hardCodedStrings.test.tsx`'s `SCREENS`

## 4. Tests & Verification

- [x] 4.1 Component tests (IPC mocked via `mockIPC`): step transitions, option validation and prefill, selection/flag interactions incl. the overwrite toggle, progress rendering, cancellation, stale-plan recovery, report rendering
- [x] 4.2 Annotate each new `#### Scenario:` with `// @covers <id>` copied verbatim from `npm run spec:trace -- --report` directly above the verifying test; every scenario in this change is automatable via the scripted provider, so no new waivers
- [x] 4.3 Sync the `translate-workflow` delta into `openspec/specs/translate-workflow/spec.md` during implementation; archive later with `--skip-specs`
- [x] 4.4 `npm run bindings:generate` and commit `src/types/bindings.ts`; `npm run verify` clean end to end (tsc, both 85 % coverage gates — close gaps with tests, never exclusions — spec:trace, bindings:check); `cargo clippy` clean
- [x] 4.5 Manual end-to-end verification with a local LLM (Ollama): mixed sources incl. an archive, glossary steering, replace mode with backups, cancellation mid-batch, an induced per-file failure
