## 1. Flatpak build assets

- [x] 1.1 Create `flatpak/manifest.json` in the **flatpak-builder manifest format** (verified against the flatpak-builder man page): top-level `app-id`/`id`, `runtime: org.gnome.Platform` + `runtime-version: 23.08`, `runtime-extensions: ["org.gnome.Platform.Extension.WebKit//23.08"]`, `sdk: org.gnome.Sdk`, `command: "subx"`, `finish-args` (`--share=ipc`, `--share=network`, `--socket=wayland`, `--socket=x11`), `build-options.build-args: ["--share=network"]`, and one `subx` module with `buildsystem: "simple"`, `subdir: "src-tauri"`, a **module-level** `sources` dir-source `{"type": "dir", "path": "..", "skip": [".git", "node_modules"]}` (a `dir` source's `path` resolves relative to the manifest file's directory `flatpak/`, so `..` = repo root; the prebuilt `dist/` is kept), a single chained build-command that pins `RUSTUP_HOME`/`CARGO_HOME` to `$FLATPAK_BUILDER_BUILDDIR`, installs a **pinned** Rust toolchain (`--default-toolchain 1.97.1`), sources `$CARGO_HOME/env`, and runs `cargo build --release`; `post-install` runs with cwd inside the module build dir (`src-tauri`), so it copies `target/release/subx` to `/app/bin/subx`, `../flatpak/subx.desktop` to `/app/share/applications/im.chenj.subx.desktop`, `icons/128x128.png` to `/app/share/icons/hicolor/128x128/apps/im.chenj.subx.png`, and `../flatpak/subx.metainfo.xml` to `/app/share/metainfo/im.chenj.subx.metainfo.xml`.
- [x] 1.2 Create `flatpak/subx.desktop` (minimal `[Desktop Entry]`: `Name=SubX`, `Exec=subx`, `Icon=im.chenj.subx`, `Categories=Utility;` — canonical app-id icon name).
- [x] 1.3 Create `flatpak/subx.metainfo.xml` (minimal AppStream `<component type="desktop">` with `<id>im.chenj.subx</id>`, name, summary, description, homepage URL, icon, `Utility` category, `<launchable>subx.desktop</launchable>`).
- [ ] 1.4 Dry-run the full flow (locally if flatpak is available, otherwise on CI): `npm ci && npm run build`, `sudo flatpak remote add --if-not-present flathub https://dl.flathub.org/repo/flathub.flatpakrepo`, then with ephemeral dirs under `$RUNNER_TEMP` (so the dir source never copies its own build output back into itself): `BUILD_DIR="${RUNNER_TEMP}/subx-flatpak-build"`, `REPO_DIR="${RUNNER_TEMP}/subx-flatpak-repo"`, `flatpak-builder --repo="$REPO_DIR" --install-deps --install-deps-from=flathub "$BUILD_DIR" flatpak/manifest.json`, `flatpak build-bundle "$REPO_DIR" subx.flatpak im.chenj.subx`. If the cargo step fails, probe the sandbox build environment (documented vars: `FLATPAK_BUILDER_BUILDDIR`, `FLATPAK_ID`, `FLATPAK_ARCH`) and adjust `RUSTUP_HOME`/`CARGO_HOME`/PATH and the `post-install` paths (cwd = module build dir `src-tauri`).
  - **Status: deferred to CI (task 4.3).** Locally `flatpak-builder` needs `sudo dnf install` (the C project's meson configure requires the `appstreamcli compose` addon, which is not installed). The `workflow_dispatch` dry run on a clean `ubuntu-24.04` runner is the clean-environment validation of the manifest.

## 2. Release workflow

- [x] 2.1 Fix the `prepare` job's `version` output in `.github/workflows/release.yml` (line 45): `version: ${{ steps.check_tag.outputs.version }}` → `version: ${{ steps.release_notes.outputs.version }}`, since `scripts/check-release-tag.mjs` never writes `GITHUB_OUTPUT` and only the `release_notes` step writes `version=`.
- [x] 2.2 Add the `build-flatpak` job to `.github/workflows/release.yml` per `design.md`: `needs: prepare`, `if: always() && (needs.prepare.result == 'success' || needs.prepare.result == 'skipped')`, pinned `ubuntu-24.04` (no `-latest`), per-job permissions (`contents: write`, `id-token: write`, `attestations: write`), steps: install `flatpak` + `flatpak-builder` via apt; `sudo flatpak remote add --if-not-present flathub https://dl.flathub.org/repo/flathub.flatpakrepo` (sudo: system remotes live under /etc/flatpak and the runner user is non-root); `npm ci && npm run build`; build in the sandbox with ephemeral dirs under `$RUNNER_TEMP` (`BUILD_DIR="${RUNNER_TEMP}/subx-flatpak-build"`, `REPO_DIR="${RUNNER_TEMP}/subx-flatpak-repo`) so the module's `dir` source never re-copies its own build output: `flatpak-builder --repo="$REPO_DIR" --install-deps --install-deps-from=flathub "$BUILD_DIR" flatpak/manifest.json` + `flatpak build-bundle "$REPO_DIR" subx.flatpak im.chenj.subx`; `actions/attest-build-provenance@v3` on `subx.flatpak`; `gh release upload "v${{ needs.prepare.outputs.version }}" subx.flatpak --clobber` gated on `github.event_name == 'push'`; `actions/upload-artifact@v6` for the bundle gated on `workflow_dispatch`. Every new `run` step declares `shell: bash`.
- [x] 2.3 Keep all existing `scripts/release-config.test.ts` assertions green (pinned runners, no `-latest`, only `GITHUB_TOKEN`, empty top-level permissions, per-job permissions, provenance on published files).

## 3. Spec traceability

- [x] 3.1 Sync the delta spec into the main spec: append the new requirement "Releases include a Linux Flatpak bundle" and its five scenarios to `openspec/specs/release-pipeline/spec.md`.
- [x] 3.2 Run `npm run spec:trace -- --report` and record the exact scenario IDs for the new requirement.
- [x] 3.3 Add assertions in `scripts/release-config.test.ts` with `// @covers release-pipeline/releases-include-a-linux-flatpak-bundle#<scenario-slug>` annotations, one per scenario. Per the review, scope **every** flatpak workflow assertion to the `build-flatpak` job block (extract via regex `build-flatpak:([\s\S]*$)`) rather than matching text fragments workflow-wide:
  - `#a-flatpak-bundle-is-published-with-every-release`: assert the workflow contains the `flatpak-builder` build step and the `gh release upload` step.
  - `#a-manual-dispatch-keeps-the-bundle-as-an-artifact`: assert the upload-artifact step is gated on `workflow_dispatch`.
   - `#the-bundle-is-built-in-a-sandbox-with-its-own-runtime`: assert the manifest uses `org.gnome.Platform` 23.08 + `org.gnome.Platform.Extension.WebKit` + `org.gnome.Sdk` and the pinned rustup toolchain module (read `flatpak/manifest.json` in the test).
  - `#a-re-run-replaces-the-bundle-asset`: assert the upload step uses `--clobber`.
  - `#the-bundle-carries-build-provenance`: assert the flatpak job runs `actions/attest-build-provenance@v3` with `subject-path: subx.flatpak`.
- [x] 3.4 Run `npm run spec:trace` (without `--report`) and confirm all scenarios in the main spec are covered (no missing `@covers` entries).

## 4. Verification & archive

- [ ] 4.1 Run `npx vitest run scripts/release-config.test.ts` and confirm all assertions (old + new) pass.
- [ ] 4.2 Run `npm run verify` (the primary gate: `tsc --noEmit`, `test:coverage`, `test:rust:coverage`, `spec:trace`, `bindings:check`, `icons:check`).
- [ ] 4.3 Trigger a manual `workflow_dispatch` of the Release workflow on GitHub to dry-run the `build-flatpak` job; confirm `subx.flatpak` is produced, the bundle is kept as a workflow artifact, and the attestation is attached.
- [ ] 4.4 (Optional, from review) Smoke-test the bundle on the runner: `flatpak install --user subx.flatpak` then `xvfb-run -a timeout 30 flatpak run im.chenj.subx` (headless display via Xvfb).
- [ ] 4.5 Archive the change with `openspec archive release-flatpak --skip-specs` (spec already synced in 3.1).
